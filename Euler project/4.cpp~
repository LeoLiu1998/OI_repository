#include <cstdio>
#include <cstdlib>
#include <cmath>
#include <vector>
#include <algorithm>
#include <iostream>
using namespace std;
int ans=0;
bool check(int);
int main(){
	for(int i=100;i<=999;i++){
		for(int j=100;j<=999;j++) {
			if(check(i*j)){
				ans=max(ans,i*j);
			}
		}
	}
}
